Android Java demo. Open in Android Studio and run.

## Tests
- Unit tests that depend on Android/NFC runtime are marked with `@Ignore` in `app/src/test/java`.
- If you want to exercise those scenarios, migrate them to `app/src/androidTest/java` and run instrumentation tests.
